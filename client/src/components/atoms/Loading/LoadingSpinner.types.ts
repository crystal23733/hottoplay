export type SpinnerSize = 'sm' | 'md' | 'lg';

/**
 * @interface LodingSpinnerProps - 로딩 스피너 컴포넌트 프로퍼티
 * @property {SpinnerSize} size - 로딩 스피너 크기
 */
export interface LodingSpinnerProps {
  size?: SpinnerSize;
}
