import { CombinationStatistics } from '@/api/megamillions/megaMillions.types';

/**
 * @property {CombinationStatistics} stats - 조합 통계 정보
 */
export default interface CombinationHistoryProps {
  stats: CombinationStatistics;
}
